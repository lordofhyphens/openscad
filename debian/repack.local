rm icons/flattr.png

# not completely nonfree, but several files without clear origin and license
# indication (LogicLib.nsh, git-archive-all.py)
rm scripts

# just called "BSD licensed" by original author, and only needed on mac anyway
rm src/SparkleAutoUpdater.*
